export /**
 * Represents an RGB color with integer channel values.
 */
interface RgbColor {
  /** Red channel value (0-255) */
  readonly r: number;
  /** Green channel value (0-255) */
  readonly g: number;
  /** Blue channel value (0-255) */
  readonly b: number;
}
